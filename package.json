{
  "name": "pos-system",
  "version": "1.0.0",
  "description": "Complete POS System with Electron POS and React Admin Panel",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run admin\" \"npm run pos\"",
    "backend": "cd backend && npm start",
    "admin": "cd admin && npm start",
    "pos": "cd pos && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../admin && npm install && cd ../pos && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["pos", "electron", "react", "nodejs"],
  "author": "",
  "license": "MIT"
}
